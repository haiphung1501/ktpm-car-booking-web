export type Car = {
  brand: string;
  model: string;
  color: string;
  type: 'car' | 'motorbike' | 'bus';
  licensePlate: string;
  capacity: number;
  car_verified: boolean;
  is_delete: boolean;
};
